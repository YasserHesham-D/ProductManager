<Window x:Class="ProductManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Product Manager" Height="600" Width="900"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" Text="Product Management System" 
                   FontSize="24" FontWeight="Bold" 
                   Margin="0,0,0,20" HorizontalAlignment="Center"/>
        <Button Content="Copy App to Desktop" Width="130" Click="CopyToDesktop_Click" Margin="710,5,40,16" />


        <!-- Search/Filter Section -->
        <Border Grid.Row="1" BorderBrush="#CCCCCC" BorderThickness="1" 
                Padding="10" Margin="0,0,0,10" CornerRadius="5">
            <StackPanel>
                <TextBlock Text="Search &amp; Filter" FontWeight="Bold" Margin="0,0,0,5"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBox x:Name="SearchBox" Grid.Column="0" 
                             Height="30" VerticalContentAlignment="Center"
                             TextChanged="SearchBox_TextChanged"/>


                    <Button Grid.Column="2" Content="Clear Filter" 
                            Width="100" Height="30" Margin="10,0,0,0"
                            Click="ClearFilter_Click"/>
                </Grid>
            </StackPanel>
        </Border>

        <!-- Products DataGrid -->
        <DataGrid x:Name="ProductsGrid" Grid.Row="2" 
                  AutoGenerateColumns="False"
                  SelectionMode="Single"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  AlternatingRowBackground="#F5F5F5"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  RowHeight="45"
                  FontSize="14" SelectionChanged="ProductsGrid_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="Padding" Value="10,0"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="Padding" Value="10,0"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTextColumn Header="Price" Binding="{Binding Price, StringFormat=C}" Width="100">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="Padding" Value="10,0"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="200">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="Padding" Value="10,0"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" 
                    HorizontalAlignment="Center" Margin="0,10,0,0">
            <Button Content="Add Product" Width="120" Height="35" 
                    Margin="5" Click="AddProduct_Click"
                    Background="#4CAF50" Foreground="White" 
                    FontWeight="Bold" Cursor="Hand"/>
            <Button Content="Edit Product" Width="120" Height="35" 
                    Margin="5" Click="EditProduct_Click"
                    Background="#2196F3" Foreground="White" 
                    FontWeight="Bold" Cursor="Hand"/>
            <Button Content="Delete Product" Width="120" Height="35" 
                    Margin="5" Click="DeleteProduct_Click"
                    Background="#F44336" Foreground="White" 
                    FontWeight="Bold" Cursor="Hand"/>
            <!--<Button Content="Open POS" Click="OpenPOS_Click" Width="120" Height="35"  Margin="100,0,0,0" Background="black" Foreground="White"  FontWeight="Bold" Cursor="Hand" />-->

        </StackPanel>
    </Grid>
</Window>