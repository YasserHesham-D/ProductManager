<Window x:Class="ProductManager.UnitDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Unit" Height="183" Width="249" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,8">
            <TextBlock Text="Unit Name" FontWeight="Bold" />
            <TextBox x:Name="NameBox" Height="30" />
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0,0,0,8"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="Save" Width="100" Click="Save_Click" Margin="6"/>
            <Button Content="Cancel" Width="100" Click="Cancel_Click" Margin="6"/>
        </StackPanel>
    </Grid>
</Window>
