<Window x:Class="ProductManager.UnitListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Manage Units" Height="420" Width="520" WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,8">
            <TextBox x:Name="FilterBox" Width="300" Height="28" VerticalContentAlignment="Center"
                    TextChanged="FilterBox_TextChanged"/>
        </StackPanel>

        <DataGrid x:Name="UnitsGrid" Grid.Row="1" AutoGenerateColumns="False" CanUserAddRows="False"
                  IsReadOnly="True" SelectionMode="Single" RowHeight="36">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="70"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="ConversionRate" Binding="{Binding ConversionRate}" Width="140" 
                                   />
                <DataGridTemplateColumn Header="Actions" Width="180">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Content="Edit" Click="EditUnit_Click" Margin="4" Width="60"/>
                                <Button Content="Delete" Click="DeleteUnit_Click" Margin="4" Width="60"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Add Unit" Click="AddUnit_Click" Width="100" Margin="6"/>
            <Button Content="Save" Click="SaveClose_Click" Width="120" Margin="6"/>
            <Button Content="Cancel" Click="Cancel_Click" Width="100" Margin="6"/>
        </StackPanel>
    </Grid>
</Window>
